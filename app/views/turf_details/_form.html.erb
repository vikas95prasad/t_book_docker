<%= form_with(model: turf_detail, local: true) do |form| %>
  <% if turf_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(turf_detail.errors.count, "error") %> prohibited this turf_detail from being saved:</h2>

      <ul>
      <% turf_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_area :address %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
